version: "1.0.0"
services:
  demo:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8023:22"
    command: ["/bin/bash"]
    tty: true
    stdin_open: true
    volumes:
      - .:/app
